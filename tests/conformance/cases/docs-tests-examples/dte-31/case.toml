id = '''DTE-31'''
area = '''docs-tests-examples'''
title = '''No test in the repository executes the CLI binary; RAW output shape and ~12 other behaviours are untested'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''test-gap'''
source_repro = '''repro/docs-tests-examples/raw-indentation/'''
command = "cd .../repro/docs-tests-examples/raw-indentation && abstract compile data RAW | cat -A\ncd <crate> && cargo test 2>&1 | grep -E '^(running|test result)'"
expected_kind = '''undecided'''
expected_note = '''At least one integration test per CLI subcommand and flag, and an exact-string assertion for RAW output.'''
actual_0_2_0 = '''cargo test → `running 17 tests` (unit) + `running 49 tests` (compiler) = 66, none of which spawn the binary; `abstract compile data RAW` emits `data: [` then `{` at column 0 while the suite reports green.'''
spec_ref = '''8.6, 8.7, 11.3'''
status = '''ready'''
expectation_1_0 = '''
compile data RAW -> expected.abraw   (11.2 names the RAW golden 'out.abraw')
Identical tree and expectation to DTE-11. The test-gap the case reports is answered by 11.3,
which requires the suite to exercise every command and every reachable E8xx identifier through
the binary, and by 11.2, which compares stdout and stderr byte for byte.
'''
