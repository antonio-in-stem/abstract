// PROPOSED (does not compile today): schema versioning + overlay declaration
// schema Sticker @v2 { ... }
// migrate Sticker 1 -> 2 { rename .applicable -> .item_pool }
// overlay v2 { since: "3.1" }

schema Sticker {
    id: text(1..40)
    rarity: enum(rare, epic, mythic)
    legacy_flag: text @optional
    icon: file(png) @optional
}
