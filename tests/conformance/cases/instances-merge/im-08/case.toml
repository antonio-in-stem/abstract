id = '''IM-08'''
area = '''instances-merge'''
title = '''A `&clone.*` line written after the instance header is silently discarded (or silently applied to the NEXT instance)'''
severity = '''major'''
verdict = '''confirmed'''
kind = '''bug'''
source_repro = '''repro/instances-merge/im-6/'''
command = '''`abstract compile data JSON` ; `abstract lint data`'''
expected_kind = '''error'''
expected_note = '''Either the clone applies (orphan.tier == 5) or a compile error: 'clone reference must appear before the instance header'.'''
actual_0_2_0 = '''EXIT=0, orphan emits "tier": 1 (the schema default) — the `&base.*` line had no effect at all. `abstract lint data` prints `abstract: ok`.'''
spec_ref = '''5.7, 5.1, 10.4 (E404)'''
status = '''ready'''
spec_note = '''orphan.ab writes "&base.*" after two body statements: E404 (5.7). A clone that resolves to nothing is never silent, and a clone is never re-attached to the next instance.'''
