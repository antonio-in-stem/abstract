id = '''IM-22'''
area = '''instances-merge'''
title = '''`#prefix*` expands inside brackets but hard-errors when written bare, despite single-value-to-list coercion being documented as equivalent'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''bug'''
source_repro = '''repro/instances-merge/im-19/'''
command = '''from im-19 `abstract compile data JSON`; from im-19/control `abstract compile data JSON`.'''
expected_kind = '''undecided'''
expected_note = '''Both forms expand identically (search + sync).'''
actual_0_2_0 = '''im-19: `abstract: things/bare.ab: Enum mismatch at Thing.caps.id: received 's*', expected one of: search, sync, export.` (EXIT=1).\nim-19/control: EXIT=0 with caps = [search/stable, sync/stable] and flags = [hat_red, hat_blue].'''
spec_ref = '''4.6, 4.8, 5.5, 5.6, 5.10, 8.3'''
status = '''ready'''
expected_files = '''expected.json (root tree); control/expected.json'''
spec_note = '''Both trees are accepted and carry the same data. 5.10 fixes the order of interpretation: enum wildcard expansion and "#tag" expansion run before single-value-to-list coercion, so an unbracketed "#s*" replicates the whole element exactly as "[#s*]" does; 5.6 position (c) does not require brackets. "availability" takes its schema default in every produced element (4.10, 7.3 step 4).'''
