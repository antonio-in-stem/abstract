id = '''IM-19'''
area = '''instances-merge'''
title = '''A tagged group list accepts duplicate tag values, so a keyed list can carry conflicting entries for the same key'''
severity = '''design'''
verdict = '''confirmed'''
kind = '''design-flaw'''
source_repro = '''repro/instances-merge/im-15/'''
command = '''`abstract compile data JSON`'''
expected_kind = '''undecided'''
expected_note = '''Duplicate tag 'search' reported (three of the four resulting entries share it), or documented as legal with defined precedence.'''
actual_0_2_0 = '''EXIT=0 with four caps entries: search/alpha, search/stable, search/stable (from `#s*`) and sync/stable — three rows keyed 'search' with conflicting availability.'''
spec_ref = '''5.5, 5.6, 4.8, 10.4 (E444)'''
status = '''ready'''
spec_note = '''5.5: within one list value, two elements whose tag field normalises to the same text are E444, and the rule is applied after wildcard expansion (5.6). Here "#s*" expands to search and sync, so three elements carry the key "search". A keyed list has at most one element per key, which is what makes clone merging and consumer lookup well defined (5.7).'''
