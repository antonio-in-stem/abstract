id = '''IM-14'''
area = '''instances-merge'''
title = '''Non-ASCII identifiers are not normalized: schema fields and enum values become unreachable mixed-case names'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''bug'''
source_repro = '''repro/instances-merge/im-14/ (also references: repro/instances-merge/im-31/)'''
command = '''`abstract compile data JSON`.\nrepro/instances-merge/im-31 — schema Thing { name; lang: enum(ES_MX, ESPAÑOL) }, instance `lang: español`. Command (run from im-31): `abstract compile data JSON`'''
expected_kind = '''undecided'''
expected_note = '''`tamaño` matches `TAMAÑO` and `español` matches `ESPAÑOL` (full Unicode lowercasing).'''
actual_0_2_0 = '''im-14: `abstract: things/x.ab: Unknown field 'tamaño' at Thing. Did you mean 'tamaÑo'? Declared fields: nombre, tamaÑo, template, id.` (EXIT=1).\nim-31: `abstract: things/x.ab: Enum mismatch at Thing.lang: received 'español', expected one of: es_mx, espaÑol. Did you mean 'espaÑol'?` (EXIT=1).'''
spec_ref = '''2.4, 3.3, 4.4.5, 10.2 (E206)'''
status = '''ready'''
expected_files = '''expected-error.txt (root tree); im-31/expected-error.txt'''
spec_note = '''Non-ASCII characters are not identifier characters (3.3), which is deliberate so that normalisation is total and locale-independent; every such character is E206. Root tree: the schema field "TAMANO" written with N-tilde, then the matching path segment in data/things/x.ab - two P1 diagnostics, ordered by the sorted source path of 2.4 (data/templates/Thing.abt before data/things/x.ab). im-31: the enum member spelled with N-tilde is E206 at the schema, so P4 never runs and the instance value is not reached.'''
