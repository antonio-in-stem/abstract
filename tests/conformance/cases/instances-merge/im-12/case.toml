id = '''IM-12'''
area = '''instances-merge'''
title = '''Tuple-array wildcards do not normalize the prefix, so `ES_*` / `es-*` silently expand to nothing while the same prefix works in a plain enum list'''
severity = '''major'''
verdict = '''confirmed'''
kind = '''bug'''
source_repro = '''repro/instances-merge/im-5/'''
command = '''`abstract compile data JSON`'''
expected_kind = '''undecided'''
expected_note = '''Both expand to es_es and es_mx (or both error identically).'''
actual_0_2_0 = '''EXIT=0 with "langs": ["es_es","es_mx"] but "copy": [] — the tuple row disappeared with no diagnostic.'''
spec_ref = '''3.3, 5.6, 5.5 (tuple arrays), 8.3'''
status = '''ready'''
spec_note = '''5.6 expands a wildcard against normalised member names using the normalised prefix, so "ES-*" normalises to "es_" and matches es_es and es_mx in declared order. The same prefix works in position (a) - an element of an enum list field - and in position (b) - a tuple cell whose column is an enum, the row being cloned once per match. Emitted members are the normalised ones (4.4.5).'''
