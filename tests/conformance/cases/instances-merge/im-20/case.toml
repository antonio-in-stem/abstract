id = '''IM-20'''
area = '''instances-merge'''
title = '''A clone replaces list fields wholesale, including tagged (keyed) lists — there is no way to override one entry'''
severity = '''design'''
verdict = '''confirmed'''
kind = '''design-flaw'''
source_repro = '''repro/instances-merge/im-8/'''
command = '''`abstract compile data JSON`'''
expected_kind = '''undecided'''
expected_note = '''Documented clearly; for a tagged list, ideally merge per tag so child inherits search/sync and adds/overrides export.'''
actual_0_2_0 = '''EXIT=0 — child has "tags": ["c"] and "caps": [{"id":"export",...}] only; base's search and sync(beta) entries are gone with no warning.'''
spec_ref = '''5.1, 5.7, 10.4 (E403)'''
status = '''ready'''
spec_note = '''0.2.0-only source: 1.0 rejects it for a reason unrelated to the original finding. child.ab opens with "&base.*" before its header: E403 (5.1). The finding itself is answered by 5.7: clone-to-clone folding merges keyed lists by the normalised text of their tag field, while an instance's own assignment replaces the cloned list wholesale, so overriding one entry is written by restating the list.'''
