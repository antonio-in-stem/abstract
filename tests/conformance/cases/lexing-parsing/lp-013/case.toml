id = '''LP-013'''
area = '''lexing-parsing'''
title = '''Text with braces that is not a file path is silently mangled by pattern expansion'''
severity = '''major'''
verdict = '''confirmed'''
kind = '''bug'''
source_repro = '''repro/lexing-parsing/bug-08-brace-nonpath-corrupt/'''
command = '''`cd repro/lexing-parsing/bug-08-brace-nonpath-corrupt && abstract compile . JSON`'''
expected_kind = '''error'''
expected_note = '''`note: {x}.{y}` should stay the literal text `{x}.{y}` (it contains no comma, so there is nothing to alternate) or be a compile error.'''
actual_0_2_0 = '''source: `note: {x}.{y}`
output: `"note": "x}.{y"` — the leading `{` and trailing `}` are deleted and the interior kept verbatim. Exit 0.'''
spec_ref = '''5.8, 5.10, 3.5 (bare text)'''
status = '''ready'''
spec_note = '''5.8: "For every other field type, { and } are ordinary characters with no expansion." The two groups are balanced, so the whole run is one bare_text and the text reaches the output unchanged.'''
