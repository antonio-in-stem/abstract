id = '''LP-031'''
area = '''lexing-parsing'''
title = '''Diagnostics list `id` twice in "Declared fields", and a duplicate tuple column reports a misleading "missing field" error'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''ux'''
source_repro = '''repro/lexing-parsing/bug-40-dup-field-in-diagnostic/ (also references: repro/lexing-parsing/bug-27-tuple-dup-columns/)'''
command = '''`cd repro/lexing-parsing/bug-40-dup-field-in-diagnostic && abstract compile . JSON` and `cd repro/lexing-parsing/bug-27-tuple-dup-columns && abstract compile . JSON`'''
expected_kind = '''undecided'''
expected_note = '''(a) `Declared fields: id, note, template` — each name once. (b) `duplicate column 'key' in tuple array 'rows'` naming the line.'''
actual_0_2_0 = '''(a) `abstract: data/things/x.ab: Unknown field 'nope' at Thing. Did you mean 'note'? Declared fields: id, note, template, id.` (schema declares only `id` and `note`).
(b) source `rows(key, key): (a, b), (c, d)` -> `abstract: data/things/x.ab: Missing required field Thing.rows.value. Add 'value: ...' to the instance, provide it as a header tag when appropriate, or mark the schema field @optional / give it a default in the .abt file.`'''
spec_ref = '''5.5, 5.12, 9.8, 10.4 (E409, E417)'''
status = '''ready'''
expected_files = '''expected-error.txt (root tree); bug-27-tuple-dup-columns/expected-error.txt'''
spec_note = '''Root tree: "nope" is not declared, so E409, whose note lists the declared fields; 5.12 requires that list to omit "template" and "id" at root level even when the schema declares "id", so the doubled "id" of 0.2.0 is unspellable. Second tree: "rows(key, key)" repeats a tuple column, which is E417 (5.5) and not a missing-field diagnostic.'''
