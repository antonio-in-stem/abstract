id = '''LP-024'''
area = '''lexing-parsing'''
title = '''Clone target ids are case-sensitive while every other identifier is lowercased'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''design-flaw'''
source_repro = '''repro/lexing-parsing/bug-23-clone-id-case/'''
command = '''`cd repro/lexing-parsing/bug-23-clone-id-case && abstract compile . JSON`'''
expected_kind = '''undecided'''
expected_note = '''Either `&base.*` resolves `@id.Base` (ids normalized like every other identifier), or the error names the case mismatch: "no instance 'base'; did you mean 'Base'?".'''
actual_0_2_0 = '''base.ab = `Thing :: @id.Base`; child.ab = `&base.*` + `Thing :: @id.child`.
output: `abstract: data/things/child.ab:1: Unknown clone target 'base'. Clone references such as '&base.*' resolve by instance id; make sure the source .ab is available next to this file or inside the compiled project.` Exit 1.'''
spec_ref = '''5.1, 5.7, 10.4 (E403)'''
status = '''ready'''
spec_note = '''0.2.0-only source: 1.0 rejects it for a reason unrelated to the original finding. 5.7 puts clones immediately after the header, so the "&base.*" written on line 1 is a clone statement before the first header, which is E403 (5.1). The case-insensitive clone target that the finding is about (5.7: "The id is normalised, so clone references are case-insensitive") is exercised by an accepted case elsewhere.'''
