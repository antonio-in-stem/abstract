id = '''LP-029'''
area = '''lexing-parsing'''
title = '''Field names differing only in case or in `-` vs `_` silently overwrite each other'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''ux'''
source_repro = '''repro/lexing-parsing/bug-16-case-collision/'''
command = '''`cd repro/lexing-parsing/bug-16-case-collision && abstract compile . JSON`'''
expected_kind = '''error'''
expected_note = '''A compile error naming the duplicate assignment and both source lines, or at least a warning.'''
actual_0_2_0 = '''source:
```
Thing :: @id.x
    Name: First
    name: Second
```
output: `"name": "Second"` — `First` is gone. Exit 0.'''
spec_ref = '''3.3, 5.4, 5.12, 5.13, 10.4 (E429)'''
status = '''ready'''
spec_note = '''"Name" and "name" normalise to the same identifier (3.3), so the two statements write the same path with intersecting applicability sets: E429, which replaces 0.2.0's silent last-wins (5.4). Disjoint version annotations are the only way to write one path twice (5.13).'''
