id = '''cli-20'''
area = '''cli-project'''
title = '''Extra positional paths are silently dropped and never validated'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''design-flaw'''
source_repro = '''repro/cli-project/case-flags/'''
command = '''../../project/target/release/abstract.exe compile case-flags/data case-flags/nonexistent-dir JSON'''
expected_kind = '''error'''
expected_note = '''Either compile every path given (like the direct form) or error 'unexpected argument case-flags/nonexistent-dir'.'''
actual_0_2_0 = '''exit 0, compiles case-flags/data only; the nonexistent second path is never even checked.'''
spec_ref = '''9.1, 9.2, 9.6, 10.8'''
status = '''ready'''
command_1_0 = '''compile data nonexistent-dir JSON'''
expected_1_0 = '''Every positional is recognised and checked; nothing is silently dropped (SPEC 9.1). JSON is the last positional with another positional present and is a format keyword, so it is the format, leaving two input paths. nonexistent-dir does not exist, which is E806 (SPEC 9.2), exit code 2. Had it existed, the invocation would name two directories and be E807 instead, because SPEC 9.2 allows exactly one directory.'''
