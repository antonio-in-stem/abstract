id = '''cli-18'''
area = '''cli-project'''
title = '''An empty or wrong-but-existing directory compiles to `{"data": []}` and lints as ok'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''design-flaw'''
source_repro = '''repro/cli-project/case-empty/'''
command = '''../../project/target/release/abstract.exe compile case-empty/data JSON
../../project/target/release/abstract.exe lint case-empty/data'''
expected_kind = '''error'''
expected_note = '''A warning on stderr (or a non-zero exit / --require-sources flag) when no .ab/.abt file was found under the given path.'''
actual_0_2_0 = '''compile prints {"data": []} exit 0; lint prints 'abstract: ok' exit 0.'''
spec_ref = '''2.3, 2.4, 9.6, 10.1'''
status = '''ready'''
command_1_0 = '''compile data JSON   (identical outcome for: lint data)'''
expected_1_0 = '''If discovery collects zero source files, compilation fails with E103 and an empty directory is never a valid empty project (SPEC 2.4), exit code 1. lint fails the same way, so it can no longer print ok for a path that contains nothing. The case directory carried no inputs because the repro directory was empty, so data/ with a .gitkeep placeholder was added here: the placeholder has neither the .ab nor the .abt extension, so it is not collected and the count of discovered sources is still zero.'''
