id = '''cli-21'''
area = '''cli-project'''
title = '''The `data` project-root marker is an exact lowercase string match, so `Data`/`DATA` silently resolves assets to the wrong folder'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''bug'''
source_repro = '''repro/cli-project/case-capdata/ (also references: repro/cli-project/case-capdata2/)'''
command = '''../../project/target/release/abstract.exe compile case-capdata/Data JSON
... compile case-capdata2/data JSON'''
expected_kind = '''document'''
expected_note = '''Same result for Data and data on a case-insensitive filesystem (or an explicit project-root flag).'''
actual_0_2_0 = '''Data: "File not found at Item.icon: './textures/icon.png' resolved to '\\?\C:\...\case-capdata\Data\assets\textures\icon.png'" exit 1. data: compiles, exit 0.'''
spec_ref = '''2.3, 2.6, 5.9, 7.6'''
status = '''ready'''
command_1_0 = '''compile Data JSON   (variant: compile case-capdata2/data JSON; the two must produce identical bytes)'''
expected_1_0 = '''The data directory marker is compared case-insensitively (SPEC 2.3, 2.6), so Data is the data directory, the project root is its parent and assets resolve under that parent. The capitalised and the lower-case tree therefore compile to the same bytes, which is the determinism guarantee of SPEC 7.6.'''
