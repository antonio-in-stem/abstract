id = '''cli-27'''
area = '''cli-project'''
title = '''`templates` reports success on projects that cannot compile and silently ignores --skip-assets/--allow-unknown'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''ux'''
source_repro = '''repro/cli-project/case-hidden/'''
command = '''../../project/target/release/abstract.exe templates case-hidden/data
../../project/target/release/abstract.exe lint case-hidden/data'''
expected_kind = '''ok'''
expected_note = '''templates surfaces the same structural errors compile does (or documents that it is a best-effort listing), and rejects flags it does not honor.'''
actual_0_2_0 = '''templates: 'Item', exit 0. lint on the same path: exit 1 'Duplicate schema Item'.'''
spec_ref = '''2.4, 7.2, 9.2, 9.6'''
status = '''ready'''
expect = '''ok'''
command_1_0 = '''lint data   (also: templates data, which prints Item on stdout)'''
expected_1_0 = '''Dot-directories and node_modules are skipped during discovery (SPEC 2.4), so the duplicate schema and the duplicate id are gone and the project is clean: lint exits 0 and prints the single line abstract: ok on stderr and nothing on stdout. templates runs the same phases P0 to P3 (SPEC 9.2), so it can no longer print a list for a project that does not compile; here it prints Item. templates takes exactly one path and no flags, so templates data --skip-assets would be E802 (Appendix A71).'''
