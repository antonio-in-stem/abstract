id = '''cli-05'''
area = '''cli-project'''
title = '''Mixing a .ab file and a directory silently drops every instance in the directory (exit 0)'''
severity = '''major'''
verdict = '''confirmed'''
kind = '''bug'''
source_repro = '''repro/cli-project/case-mixed/'''
command = '''../../project/target/release/abstract.exe case-mixed/one/a.ab case-mixed/two JSON
../../project/target/release/abstract.exe case-mixed/one case-mixed/two JSON'''
expected_kind = '''error'''
expected_note = '''Either emit a + b + c (explicit directory arguments are requests, not context), or refuse to mix file and directory inputs with an error.'''
actual_0_2_0 = '''exit 0, output contains only "id": "a"; b and c are silently absent. The all-directories form correctly emits a, b, c.'''
spec_ref = '''2.3, 2.5, 9.2, 9.6, 10.8'''
status = '''ready'''
command_1_0 = '''compile one/a.ab two JSON   (second invocation: compile one two JSON)'''
expected_1_0 = '''Mixing a file and a directory is E807 (SPEC 2.5), exit code 2, so instances can no longer be silently dropped. The second invocation names two directories, which is also E807: SPEC 9.2 allows exactly one directory, and one and two resolve to two different project roots because neither has a data directory above or below it (SPEC 2.3).'''
