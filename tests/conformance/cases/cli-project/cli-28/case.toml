id = '''cli-28'''
area = '''cli-project'''
title = '''No tests exist for the CLI layer: argument parsing, init, --out, direct-form ordering and project-root discovery are entirely uncovered'''
severity = '''major'''
verdict = '''confirmed'''
kind = '''test-gap'''
source_repro = '''repro/cli-project/case-flags/'''
command = '''cd <crate> && cargo test
../../project/target/release/abstract.exe compile case-flags/data JSON --out=case-flags/out1.json; ls case-flags/out1.json'''
expected_kind = '''document'''
expected_note = '''A CLI test module (or assert_cmd-style integration tests) covering parse_compile_request/parse_direct_request/collect_options/flag_value, init scaffolding, and the project-root and extension rules.'''
actual_0_2_0 = '''grep -c '#[test]' src/main.rs == 0; the suite is green while all of the above defects are present.'''
spec_ref = '''9.2, 9.3, 9.4, 11.3'''
status = '''ready'''
command_1_0 = '''compile data JSON   (also: compile data JSON --out=out1.json, whose file content is these same bytes)'''
expected_1_0 = '''This is a test-gap finding, so the runnable expectation is only the baseline document. What closes it is the coverage SPEC 11.3 requires: every command of SPEC 9.2 and every reachable E8xx identifier exercised through the binary, including --out, --out=, --skip-assets, --max-errors, a repeated flag, an unknown flag, an unknown format keyword, a mixed file-and-directory invocation, a refused destination and a run piped into a reader that closes stdout.'''
