id = '''cli-03'''
area = '''cli-project'''
title = '''file()/image() values are not confined to the project: `../..` traversal and absolute paths are validated on disk and emitted verbatim'''
severity = '''major'''
verdict = '''confirmed'''
kind = '''security'''
source_repro = '''repro/cli-project/case-traversal/'''
command = '''../../project/target/release/abstract.exe compile case-traversal/data JSON
../../../../../../../../Windows/win.ini
C:/Windows/win.ini'''
expected_kind = '''error'''
expected_note = '''A relative asset path that escapes <project>/assets, or an absolute path, is rejected (or at minimum requires an opt-in flag), so compiled output can only reference files inside the project.'''
actual_0_2_0 = '''exit 0; both instances compile and the compiled JSON contains "manual": "../../../../../../../../Windows/win.ini" and "manual": "C:/Windows/win.ini" verbatim.'''
spec_ref = '''2.7, 5.9, 10.4'''
status = '''ready'''
command_1_0 = '''compile data JSON'''
expected_1_0 = '''Both values are rejected before any filesystem access: items/a.ab contains parent segments and items/b.ab carries a drive prefix, and each is E424 (SPEC 5.9). Instances are compiled in the order of SPEC 2.7, so Doc/a is reported before Doc/b. Neither value can reach the compiled document.'''
