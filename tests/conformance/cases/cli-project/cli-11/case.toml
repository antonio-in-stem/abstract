id = '''cli-11'''
area = '''cli-project'''
title = '''`--skip-assets` does not skip absolute-path file()/image() values, contradicting the documentation'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''doc-mismatch'''
source_repro = '''repro/cli-project/case-skipassets-abs/'''
command = '''../../project/target/release/abstract.exe compile case-skipassets-abs/data JSON --skip-assets'''
expected_kind = '''error'''
expected_note = '''With --skip-assets, no on-disk existence or content check runs for any file()/image() value.'''
actual_0_2_0 = '''abstract: items/abs.ab: File not found at Doc.manual: 'C:/definitely/missing/manual.txt' resolved to 'C:/definitely/missing/manual.txt'. The schema declares file(...), so the file must exist. (exit 1)'''
spec_ref = '''2.7, 5.9, 9.5, 10.4'''
status = '''ready'''
command_1_0 = '''compile data JSON --skip-assets'''
expected_1_0 = '''--skip-assets skips exactly the on-disk existence check, the header probe and the dimension check, and nothing else (SPEC 9.5). items/abs.ab carries a drive prefix, which fails the confinement rule of SPEC 5.9 and is E424 whether or not the flag is given. items/rel.ab names a relative path that does not exist, and with the flag its existence check is skipped, so it raises nothing. Instances are ordered by (template, id), so Doc/abs is the one reported.'''
