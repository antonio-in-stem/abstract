id = '''cli-26'''
area = '''cli-project'''
title = '''Compiled output is printed to stdout even when --out is given, and a failed write leaves a full success payload on stdout with exit 1'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''design-flaw'''
source_repro = '''repro/cli-project/case-outfail/'''
command = '''../../project/target/release/abstract.exe compile case-outfail/data JSON --out case-outfail/nope/out.json > p_stdout.txt 2> p_stderr.txt; echo $?; cat p_stdout.txt'''
expected_kind = '''error'''
expected_note = '''With --out, write first and print nothing (or only the 'wrote ...' line); no payload on stdout when the run fails.'''
actual_0_2_0 = '''exit 1, p_stdout.txt contains the complete JSON document, p_stderr.txt contains only 'abstract: El sistema no puede encontrar la ruta especificada. (os error 3)'.'''
spec_ref = '''9.4, 9.6, 9.7, 10.8'''
status = '''ready'''
command_1_0 = '''compile data JSON --out nope/out.json'''
expected_1_0 = '''With --out, nothing at all is written to stdout (SPEC 9.4, 9.7), so a failed write cannot leave a success payload behind. The parent directory must exist, and a write failure of any kind is E810 with exit code 3 (SPEC 9.4, 9.6). stdout is empty in this run and stderr carries the diagnostic.'''
