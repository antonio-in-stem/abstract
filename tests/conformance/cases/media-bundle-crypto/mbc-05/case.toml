id = '''mbc-05'''
area = '''media-bundle-crypto'''
title = '''docs claim '--skip-assets disables all on-disk checks' but absolute paths are still opened/checked (contradicts the same file)'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''doc-mismatch'''
source_repro = '''repro/media-bundle-crypto/case05-path-traversal/'''
command = '''abstract.exe compile data JSON --skip-assets
doc: C:\Windows\does-not-exist-xyz.ini'''
expected_kind = '''undecided'''
expected_note = '''Either --skip-assets skips absolute paths too, or the doc says absolute asset paths are always checked regardless of --skip-assets.'''
actual_0_2_0 = '''Absolute existing file compiles (probed); absolute missing file fails 'File not found' under --skip-assets; docs say all checks are disabled.'''
spec_ref = '''5.9, 9.5, 10.4 (E424)'''
status = '''ready'''
expectation_1_0 = '''
compile data JSON --skip-assets -> expected-error.txt
9.5 enumerates exactly what --skip-assets suppresses: the on-disk existence check (E421), the
image header probe (E422) and the dimension check (E423). It does NOT suppress the extension
check (E420) or the path-confinement check (E424), and it has no effect on "exists". The
absolute path is therefore E424 with or without the flag, and no absolute path is ever opened,
which is the behaviour the docs and the code disagreed about.
'''
