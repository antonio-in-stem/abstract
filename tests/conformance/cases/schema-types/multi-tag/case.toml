id = '''multi-tag'''
area = '''schema-types'''
title = '''A group may declare several @tag fields; only the first is functional and the rest silently become ordinary required fields'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''design-flaw'''
source_repro = '''repro/schema-types/multi-tag/'''
command = '''...\abstract.exe compile repro/schema-types/multi-tag/data JSON'''
expected_kind = '''error'''
expected_note = '''Parse-time error: "at most one @tag field per group", at the second @tag line.'''
actual_0_2_0 = '''abstract: items/a.ab: Missing required field Thing.caps.o. Add 'o: ...' to the instance, provide it as a header tag when appropriate, or mark the schema field @optional / give it a default in the .abt file.'''
spec_ref = '''4.8, 10.3'''
status = '''ready'''
command_1_0 = '''compile data JSON'''
expected_1_0 = '''At most one field per group may carry @tag; the second is E310 (SPEC 4.8), reported at templates/Thing.abt:5 with a note at the first tagged field. P3 fails, so the instance is never compiled.'''
