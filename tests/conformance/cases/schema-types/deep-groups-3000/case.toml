id = '''deep-groups-3000'''
area = '''schema-types'''
title = '''Deeply nested schema groups blow the stack: the process aborts with 'has overflowed its stack' instead of reporting an error'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''bug'''
source_repro = '''repro/schema-types/deep-groups-3000/ (also references: repro/schema-types/deep-groups-1000/)'''
command = '''abstract compile repro/schema-types/deep-groups-3000/data JSON
(repro/schema-types/deep-groups-1000/ with 1000 levels still succeeds, so the threshold is between 1000 and 3000.)'''
expected_kind = '''error'''
expected_note = '''A CompileError such as "schema nesting too deep (max N levels)" at the offending line, exit code 1.'''
actual_0_2_0 = '''exit=127

thread 'main' (40412) has overflowed its stack'''
spec_ref = '''3.7, 7.2, 10.2'''
status = '''ready'''
command_1_0 = '''compile data JSON   (variant: compile deep-groups-1000/data JSON)'''
expected_1_0 = '''Schema group nesting is limited to 64 (SPEC 3.7) and the limit is enforced in P3 (SPEC 7.2), so both the 3000-level and the 1000-level tree are E209 naming the subject and the limit. The compiler must not abort or overflow the stack (SPEC 3.7). P4 never runs, so the E412 that items/a.ab would raise (a bare value on a group field, SPEC 5.10) is not reached.'''
