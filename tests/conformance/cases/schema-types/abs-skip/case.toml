id = '''abs-skip'''
area = '''schema-types'''
title = '''--skip-assets does not skip on-disk checks for absolute paths, contradicting the CLI help and both docs'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''doc-mismatch'''
source_repro = '''repro/schema-types/abs-skip/'''
command = '''...\abstract.exe compile repro/schema-types/abs-skip/data JSON --skip-assets'''
expected_kind = '''error'''
expected_note = '''abstract: ok / clean JSON, per the documented behaviour.'''
actual_0_2_0 = '''abstract: items/a.ab: File not found at Thing.icon: 'C:/definitely/not/here/x.png' resolved to 'C:/definitely/not/here/x.png'. The schema declares file(...), so the file must exist.   (the relative path in b.ab was correctly skipped)'''
spec_ref = '''2.3, 4.4.6, 5.9, 9.5, 10.4'''
status = '''ready'''
command_1_0 = '''compile data JSON --skip-assets  (cwd = the case directory)'''
expected_1_0 = '''items/a.ab writes an absolute value with a drive prefix, which is E424 (SPEC 5.9). --skip-assets skips only the on-disk existence check, the header probe and the dimension check (SPEC 9.5); it never skips the path-confinement check, so E424 is reported with or without the flag. items/b.ab (nope/x.png) is relative and its existence check is skipped, so it raises nothing.'''
