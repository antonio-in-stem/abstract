id = '''name-space'''
area = '''schema-types'''
title = '''A schema name containing spaces is accepted and listed by `abstract templates`, but no instance can ever reference it'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''design-flaw'''
source_repro = '''repro/schema-types/name-space/'''
command = '''...\abstract.exe compile .../name-space/data JSON  and  ...\abstract.exe templates .../name-space/data'''
expected_kind = '''error'''
expected_note = '''Parse-time error: "schema names may not contain spaces".'''
actual_0_2_0 = '''compile: abstract: items/a.ab:1: statement 'My Thing :: @id.a' appears before any instance header (expected 'Template :: ...' first)
templates: My Thing'''
spec_ref = '''3.4, 5.1, 10.2'''
status = '''ready'''
command_1_0 = '''compile data JSON   (identical outcome for: templates data)'''
expected_1_0 = '''A schema name contains no whitespace (SPEC 3.4), so templates/T.abt:1 is E208. In the instance file the text before the double colon is not a schema name, which is E439 naming that text (SPEC 5.1, and the catalogue entry for E439 uses exactly this shape). Both are P1 diagnostics and sources are diagnosed in the sorted order of SPEC 2.4, so data/items/a.ab is reported before data/templates/T.abt. templates therefore cannot list a name no instance could reference.'''
