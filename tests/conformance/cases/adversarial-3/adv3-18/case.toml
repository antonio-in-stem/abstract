id = '''ADV3-18'''
area = '''adversarial-3'''
title = '''`{ranges}` collapses an integer part whose bounds are equal and keeps both bounds of a float part: `text(1..3, 8, 12..14, 20..20)` renders `1..3, 8, 12..14, 20`'''
severity = '''minor'''
kind = '''ruling'''
verdict = '''confirmed'''
status = '''ready'''
expected_kind = '''error'''
expected_files = '''expected-error.txt'''
spec_ref = '''4.5, 9.8, 10.4'''
expected_note = '''SPEC 9.8: `{ranges}` renders part by part in declaration order, an interval as `{min}..{max}`, and "an integer part whose two bounds are equal renders as the bare number". The declaration mixes all three shapes, so the message must read `Range mismatch at atlas.tier: 5 is not in 1..3, 8, 12..14, 20.`'''
actual_1_0 = '''exit 1, exactly that message.'''
