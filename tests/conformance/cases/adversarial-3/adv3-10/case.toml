id = '''ADV3-10'''
area = '''adversarial-3'''
title = '''A project whose only source declares `versions 1..3` and nothing else compiles to an empty `data` array whose envelope carries the declared range'''
severity = '''minor'''
kind = '''spec'''
verdict = '''confirmed'''
status = '''ready'''
expected_kind = '''document'''
expected_files = '''expected.json'''
spec_ref = '''2.4, 4.12, 7.2, 8.1'''
expected_note = '''The same reading as ADV3-09: the file declares no schema and no instance, so SPEC 8.1's second bullet does not describe it, yet the compile succeeds. It also fixes that the envelope's `versions` object is taken from a file that declares nothing else, so `min` is 1 and `max` is 3.'''
actual_1_0 = '''exit 0, with `min` 1, `max` 3 and `data` empty.'''
