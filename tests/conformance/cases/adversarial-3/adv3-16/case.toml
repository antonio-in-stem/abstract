id = '''ADV3-16'''
area = '''adversarial-3'''
title = '''E523 carries the instance-header note and one `at index ..., item ....` note per enclosing loop, which SPEC 10.5 now prescribes'''
severity = '''minor'''
kind = '''spec'''
verdict = '''confirmed'''
status = '''ready'''
expected_kind = '''error'''
expected_files = '''expected-error.txt'''
spec_ref = '''3.7, 6.12, 9.8, 10.5, 11.2'''
expected_note = '''SPEC 10.5's E523 row, as revision 7 settled it, prescribes the message, a note at the instance header and one `note: at index {i}, item {item}.` for each `for` enclosing the one the position names, written outermost first, so the tail is bounded by SPEC 3.7's block-nesting limit and is at most 63 notes long. SPEC 6.12 states the same rule for every logic diagnostic raised inside a `for` body. The crossing here falls four levels down, so the diagnostic is E523 at the tenth chain's innermost `for`, followed by the instance-header note and four loop notes.'''
actual_1_0 = '''exit 1, with the diagnostic followed by `note: instance 'x'.` and four `note: at index 0, item 1.` lines, one per enclosing `for`. Nine chains cost 999 990, so the crossing charge falls on the seventh iteration of the tenth chain's innermost loop, four levels down.'''
note_1_0 = '''src/logic.rs::loop_notes appends one note per live loop binding to every diagnostic the evaluator raises, outermost first. Revision 7 kept the tail and named it in SPEC 10.5 rather than dropping it from E523: it says which iteration of every enclosing loop the crossing happened on, which the position alone does not.'''
