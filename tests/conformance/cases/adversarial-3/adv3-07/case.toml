id = '''ADV3-07'''
area = '''adversarial-3'''
title = '''An `else` and an `else if` reached across two blank lines and a comment line are joined by rule (b) and compile'''
severity = '''minor'''
kind = '''ruling'''
verdict = '''confirmed'''
status = '''ready'''
expected_kind = '''document'''
expected_files = '''expected.json'''
spec_ref = '''3.6, 6.2, 6.3'''
expected_note = '''SPEC 3.6 rule (b) tests the next token, not the next physical line, and SPEC 6.3 states that any number of blank and comment lines may sit between a `}` and its `else` or `else if`. The instance takes the `else if` branch, so `ready` is false and `count` is absent.'''
actual_1_0 = '''exit 0, with `ready` false.'''
