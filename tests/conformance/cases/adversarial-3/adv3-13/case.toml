id = '''ADV3-13'''
area = '''adversarial-3'''
title = '''A nested `$(Schema)` block spends the same budget as the instance's own block: 555 550 in the nested block and 555 550 in the outer one is E523'''
severity = '''major'''
kind = '''ruling'''
verdict = '''confirmed'''
status = '''ready'''
expected_kind = '''error'''
expected_files = '''expected-error.txt'''
spec_ref = '''3.7, 6.11, 7.3, 10.5'''
expected_note = '''SPEC 3.7: "The budget is spent by every `for` that runs while one instance is compiled for one version - the block of the instance's own schema and the block of every nested `$(Schema)` value alike." Five five-level chains cost 555 550 each, so neither block crosses the bound alone and the pair costs 1 111 100. SPEC 6.11 runs the nested block first, so the crossing charge falls inside the outer block's fifth chain. A per-evaluator budget would compile this file, which is what makes it the case that distinguishes the two readings.'''
actual_1_0 = '''exit 1: `data/templates/T.abt:119:21: error[E523]: Logic work limit exceeded at x: the logic executed more than 1000000 loop iterations in version 1.`, positioned inside the outer block's fifth chain.'''
