id = '''ADV3-17'''
area = '''adversarial-3'''
title = '''E413 against a `text` range renders the value's Unicode scalar count and never the text: an astral character and a combining mark each count one'''
severity = '''major'''
kind = '''ruling'''
verdict = '''confirmed'''
status = '''ready'''
expected_kind = '''error'''
expected_files = '''expected-error.txt'''
spec_ref = '''4.4.1, 9.8, 10.4'''
expected_note = '''SPEC 9.8: against a `text` range `{value}` is "the number of Unicode scalar values in the value, written as a decimal integer, unquoted; the text itself does not appear in the message at all", and "a combining mark counts as one and an astral character counts as one, and neither bytes nor UTF-16 code units are ever counted". The value is U+1F600, U+0065, U+0301, U+1F600, U+0061: five scalar values, four grapheme clusters, seven UTF-16 code units and eleven UTF-8 bytes. The message must read `Range mismatch at atlas.tier: 5 is not in 1..3.`'''
actual_1_0 = '''exit 1: `data/items/atlas.ab:3:5: error[E413]: Range mismatch at atlas.tier: 5 is not in 1..3.` - the count is right and the three wrong counts (4, 7, 11) are all excluded.'''
