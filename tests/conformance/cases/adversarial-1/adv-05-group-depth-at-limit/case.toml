id = '''ADV-05'''
area = '''adversarial-1'''
title = '''A schema with exactly 64 nested groups is rejected as E209, one level below the documented limit'''
severity = '''minor'''
kind = '''bug'''
verdict = '''confirmed'''
status = '''ready'''
expected_kind = '''document'''
expected_files = '''case.toml expect = ok'''
spec_ref = '''3.7, 4.7, 7.2'''
spec_note = '''SPEC 3.7 sets 'Nesting depth of schema groups (static)' at 64 and requires E209 only 'when one is exceeded'. A schema whose innermost group is the 64th is at the limit, not over it, so it must validate. 63 nested groups compile today; 64 do not.'''
actual_1_0 = '''exit 1, 'data/templates/thing.abt:66:257: error[E209]: Schema group nesting exceeds the limit of 64.' schema.rs uses `depth >= GROUP_DEPTH`, which rejects at the limit rather than above it.'''
expect = '''ok'''
