id = '''LOGIC-21'''
area = '''logic-engine'''
title = '''A derive inside a for-loop overwrites its target every iteration; there is no way to accumulate a list'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''missing-feature'''
source_repro = '''repro/logic-engine/L17-loop-derive-overwrites/'''
command = '''repro/logic-engine/L17-loop-derive-overwrites — cd repro/logic-engine/L17-loop-derive-overwrites && abstract compile data JSON'''
expected_kind = '''error'''
expected_note = '''Either seen == ["alpha","beta","gamma"], or a diagnostic that derive cannot accumulate.'''
actual_0_2_0 = '''{
  "data": [
    {
      "template": "T",
      "id": "one",
      "name": "atlas",
      "tags": [ "alpha", "beta", "gamma" ],
      "seen": [ "gamma" ]
    }
  ]
}
EXIT=0'''
spec_ref = '''6.2, 6.9, 10.2'''
status = '''ready'''
note_1_0 = ''''for t in .tags' is E210: the loop variable needs its '$' (SPEC 6.9). Written as 'for $t in .tags' the project would compile with seen = ["gamma"]: a derive inside a for executes once per element and the last write wins; Abstract 1.0 has no accumulation operator (SPEC 6.4), which the spec states rather than leaving implicit.'''
