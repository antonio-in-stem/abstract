id = '''LOGIC-28'''
area = '''logic-engine'''
title = '''No negative tests for the logic engine: every logic test in the suite is a happy path'''
severity = '''minor'''
verdict = '''partial'''
kind = '''test-gap'''
source_repro = '''repro/logic-engine/L01-typo-condition-always-true/'''
command = '''Any of the L01-L26 repro projects doubles as the missing test; e.g. cd repro/logic-engine/L01-typo-condition-always-true && abstract compile data JSON  — and, for the suite itself: cd <crate> && cargo test'''
expected_kind = '''undecided'''
expected_note = '''A logic test suite that pins rejection behaviour for malformed logic and determinism of output, not only the happy paths.'''
actual_0_2_0 = '''cargo test passes 49 compiler tests + unit tests while all 26 defects above reproduce against the same binary; no test in tests/compiler_tests.rs asserts a logic parse/eval REJECTION, and no test compiles the same sources twice to compare output.'''
spec_ref = '''6.6, 7.2, 10.5, 11.3'''
status = '''ready'''
note_1_0 = '''Same project as LOGIC-04: the conditions on lines 7 and 9 are E511 and line 8 is well formed. As a coverage case it is discharged by SPEC 11.3, which requires at least one case for each of E501-E521 and a determinism case proving two runs report the same first failing require.'''
