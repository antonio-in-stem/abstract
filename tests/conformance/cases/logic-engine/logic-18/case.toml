id = '''LOGIC-18'''
area = '''logic-engine'''
title = '''derive cannot satisfy a required field — the pre-logic validation pass rejects the instance first, and the docs' own example does not compile'''
severity = '''minor'''
verdict = '''confirmed'''
kind = '''doc-mismatch'''
source_repro = '''repro/logic-engine/L16-derive-cannot-fill-required/'''
command = '''repro/logic-engine/L16-derive-cannot-fill-required — cd repro/logic-engine/L16-derive-cannot-fill-required && abstract compile data JSON'''
expected_kind = '''document'''
expected_note = '''The logic block sets .shipping_class on every path (if/else), so the instance should compile with shipping_class "express".'''
actual_0_2_0 = '''abstract: items/one.ab: Missing required field T.shipping_class. Add 'shipping_class: ...' to the instance, provide it as a header tag when appropriate, or mark the schema field @optional / give it a default in the .abt file.
EXIT=1'''
spec_ref = '''6.11, 7.3, 8.4'''
status = '''ready'''
note_1_0 = '''The required-field check runs after logic (SPEC 7.3 step 6, SPEC 6.11 step 4), so a derive may satisfy a required field. '.kind == "pro"' is true, the then-branch derives shipping_class, and the instance compiles.'''
