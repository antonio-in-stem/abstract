id = '''R7-03'''
area = '''revision-7'''
title = '''A project whose sources declare schemas and logic but no instance compiles to an empty `data` array'''
severity = '''minor'''
kind = '''ruling'''
verdict = '''confirmed'''
status = '''ready'''
expected_kind = '''document'''
expected_files = '''expected.json'''
spec_ref = '''2.4, 7.2, 8.1, 10.1'''
expected_note = '''SPEC 8.1 named only single-file mode as a way to reach an empty `data` array, and SPEC 7.2 blesses a project with zero instances explicitly: P3 checks every schema and every logic block against no instance at all. Both are true, so SPEC 8.1 now states both, and keeps them apart from the third case - a project with no source files, which is E103 and emits no document at all.'''
actual_1_0 = '''exit 0, with `data` and `overlays` both empty.'''
