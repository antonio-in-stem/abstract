[package]
name = "abstract-lang"
version = "1.2.0"
edition = "2021"
description = "Abstract: a schema-backed data language compiler with zero dependencies."
license = "MIT"

[lib]
name = "abstract_lang"
path = "src/lib.rs"

[[bin]]
name = "abstract"
path = "src/main.rs"

# Abstract 1.0 has no dependencies and no dev-dependencies, by design.
[dependencies]

[dev-dependencies]
